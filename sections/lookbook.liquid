<section>
   {% if section.settings.heading!=blank %}
    <h2>{{ section.settings.heading}}</h2>
   {% endif %}
</section>

{% schema %}
{
    "name": "Lookbook",
    "settings": [
        {
            "type":"text",
            "id":"heading",
            "label":"section heading"
        }
    ],
    "blocks": []
}
{% endschema %}